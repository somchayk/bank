<h1>Add Account</h1>

<%= form_with(model: @account, local: true) do |f| %>
  <div class=" input-field">
    <%= f.text_field :name, required: true, placeholder: "Name" %>
  </div>

  <div class="input-field">
    <%= f.number_field :balance, min: 0, step: 0.01 %>
  </div>

  <%= f.submit nil, class: 'waves-effect wave-lights btn'%>
<% end %>

